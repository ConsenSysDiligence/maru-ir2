struct Foo {
	x: u32;
	y: u32;
}

fun alloc_foo<M>(x: u32, y: u32): Foo *M
locals res: Foo *M;
{
	entry:
		res := alloc Foo in M;
		store x in res.x;
		store y in res.y;
		return res;
}

fun main()
locals
	f2: Foo *M;
{
	entry:
		f2 := call alloc_foo<out M>(3_u32, 4_u32);
		return;
		
}
